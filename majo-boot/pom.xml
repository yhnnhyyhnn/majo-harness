<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.majo.harness</groupId>
    <artifactId>majo-harness-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>majo-boot</artifactId>
  <name>majo-boot</name>
  <description>Profile-to-loader glue: registers the shipped harness plugins as builtins and boots an entry tree from profile files</description>

  <dependencies>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-session</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-tools</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-llm</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-agent-loop</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-provider-openai</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-fs</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-subprocess</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-shell</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-sandbox</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-interaction</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-skill</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.majo.harness</groupId>
      <artifactId>majo-subagent</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jcordis</groupId>
      <artifactId>jcordis-all</artifactId>
    </dependency>
  </dependencies>
</project>
